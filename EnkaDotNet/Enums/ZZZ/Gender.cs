namespace EnkaDotNet.Enums.ZZZ
{
    /// <summary>
    /// ZZZ grammatical gender used for localized text variants (e.g. titles that contain
    /// gendered tokens like <c>{M#...}{F#...}</c>). Matches the player's chosen Proxy gender.
    /// </summary>
    public enum Gender
    {
        /// <summary>Masculine variant (token "M").</summary>
        Male,

        /// <summary>Feminine variant (token "F").</summary>
        Female
    }
}
